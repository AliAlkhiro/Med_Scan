# 7.1 Add PWA App Shell Caching Execution Report

Status: Done

## Summary

Configured Vite PWA support for a small app shell cache that helps the public app reload after the first visit on poor connectivity.

## Implemented

- Added `vite-plugin-pwa` to the Vite build.
- Added a PWA manifest with standalone display, app theme colors, and a local SVG app icon.
- Configured Workbox to precache built app shell assets only.
- Left runtime caching disabled so Supabase product lookups remain backend reads.
- Avoided attachment preloading or attachment runtime caching.

## Verification

- Ran `npm run lint`.
- Ran `npm run build`.

## Notes

- Admin navigation fallback is excluded from the service worker route fallback so protected admin URLs stay browser-driven and auth-dependent.
- The plugin install reported existing npm audit findings and an engine warning for `@zxing/library` under Node 22, but installation and verification completed.
